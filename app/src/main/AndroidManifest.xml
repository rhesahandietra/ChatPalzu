<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  package="id.handietron.aplikasichat">

	<application
		android:allowBackup="true"
		android:icon="@drawable/pinky"
		android:label="@string/app_name"
		android:roundIcon="@drawable/pinky"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity
			android:name="id.handietron.aplikasichat.MainActivity"
			android:label="@string/app_name"
			android:theme="@style/AppTheme.NoActionBar">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name="id.handietron.aplikasichat.IsiChatActivity">
		</activity>
	</application>

</manifest>